import type { BabelStore } from "../store/store.ts";
import type { Door } from "./door.ts";

/*
  THE NINE ANSWERS: feed, record, thread, topics, topic, pulse, runs, run, policy.

  STUB (Scaffold, P0): this file belongs to the store/read slice; replace it with that version.
*/

export function readDoors(_store: BabelStore): readonly Door[] {
  return [];
}

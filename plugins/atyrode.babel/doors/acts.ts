import type { BabelStore } from "../store/store.ts";
import type { Door } from "./door.ts";

/*
  THE OPERATOR'S ACTS: rule, comment, answer, interest, file, unfile, tell, setPolicy, and the
  one-off importLedger.

  STUB (Scaffold, P0): this file belongs to the acts slice; replace it with that version.
*/

export function actDoors(_store: BabelStore): readonly Door[] {
  return [];
}
